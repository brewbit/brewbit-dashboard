<%= form_for [@device, @sensor] do |f| %>
  <% if @sensor.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@sensor.errors.count, "error") %> prohibited this sensor from being saved:</h2>

      <ul>
      <% @sensor.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :setpoint_type %><br>
    <%= f.select :setpoint_type, [['Static',:static],['Temp Profile',:temp_profile]] %>
  </div>

  <div class="field">
    <%= f.label :static_setpoint %><br>
    <%= f.number_field :static_setpoint %>
  </div>

  <div class="field">
    <%= f.label :temp_profile %><br>
    <%= f.select :temp_profile, @device.user.temp_profiles.collect{|x| [x.name, x.id]} %>
  </div>
  
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
