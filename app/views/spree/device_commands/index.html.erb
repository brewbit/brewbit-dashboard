<h3><%= @device.name %> Command History</h3>

<div class="col-md-10 col-md-offset-1">
  <table class="table table-hover">
    <thead>
      <tr>
        <th class="col-md-6">Command Name</th>
        <th class="col-md-3">Sent</th>
        <th class="col-md-2"></th>
      </tr>
    </thead>
    <tbody>
      <% @device_commands.each do |device_command| %>
        <tr>
          <td><%= device_command.name %></td>
          <td><%= device_command.created_at.to_s(:pretty) %></td>
          <td>
            <div class="btn-group">
              <%= link_to raw('<span class="glyphicon glyphicon-eye-open"></span>'), device_command_path(@device, device_command), class: 'btn btn-default' %>
              <%= link_to raw('<span class="glyphicon glyphicon-trash"></span>'), device_command_path(@device, device_command), method: :delete, class: 'btn btn-danger', data: { confirm: 'Are you sure?' } %>
            </div>
          </td>
        </tr>
      <% end %>
    </tbody>
  </table>
</div>